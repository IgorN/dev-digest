import type { CSSProperties } from "react";
import { TABLE_GRID_COLS } from "./constants";

/** Co-located styles for EvalDashboardIndex. */
export const s = {
  page: { padding: "24px 32px 44px", maxWidth: 1100, margin: "0 auto" } satisfies CSSProperties,
  header: { display: "flex", alignItems: "flex-start", gap: 14, marginBottom: 24 } satisfies CSSProperties,
  headerText: { flex: 1 } satisfies CSSProperties,
  h1: { fontSize: 24, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: { fontSize: 14, color: "var(--text-secondary)", marginTop: 4 } satisfies CSSProperties,
  section: { marginBottom: 28 } satisfies CSSProperties,
  agentList: { display: "flex", flexDirection: "column", gap: 10 } satisfies CSSProperties,
  agentRowLink: { textDecoration: "none", color: "inherit", display: "block" } satisfies CSSProperties,
  agentRow: {
    display: "flex",
    alignItems: "center",
    gap: 16,
    padding: 14,
  } satisfies CSSProperties,
  agentIconBox: {
    width: 30,
    height: 30,
    borderRadius: 7,
    background: "var(--accent-bg)",
    color: "var(--accent)",
    display: "grid",
    placeItems: "center",
    flexShrink: 0,
  } satisfies CSSProperties,
  agentMain: { flex: 1, minWidth: 0 } satisfies CSSProperties,
  agentNameRow: { display: "flex", alignItems: "center", gap: 8, marginBottom: 3 } satisfies CSSProperties,
  agentName: { fontSize: 14, fontWeight: 600 } satisfies CSSProperties,
  agentMeta: { fontSize: 12.5, color: "var(--text-muted)" } satisfies CSSProperties,
  agentSparkline: { flexShrink: 0 } satisfies CSSProperties,
  metricCols: { display: "flex", gap: 18, flexShrink: 0 } satisfies CSSProperties,
  metricCol: { textAlign: "right", minWidth: 46 } satisfies CSSProperties,
  metricLabel: {
    fontSize: 10.5,
    fontWeight: 700,
    letterSpacing: "0.06em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  metricValue: (color: string): CSSProperties => ({ fontSize: 14, fontWeight: 600, marginTop: 2, color }),
  agentChevron: { flexShrink: 0, color: "var(--text-muted)" } satisfies CSSProperties,
  table: { display: "flex", flexDirection: "column" } satisfies CSSProperties,
  tableHeadRow: {
    display: "grid",
    gridTemplateColumns: TABLE_GRID_COLS,
    gap: 10,
    padding: "0 14px 8px",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.05em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  tableRow: {
    display: "grid",
    gridTemplateColumns: TABLE_GRID_COLS,
    gap: 10,
    padding: "10px 14px",
    borderTop: "1px solid var(--border)",
    fontSize: 13,
    alignItems: "center",
  } satisfies CSSProperties,
} as const;
