# docs — client

Deep-dives for the `client` package: page/data-flow walkthroughs, component
architecture notes, UI conventions that are too long for `client/CLAUDE.md`.
Linked from `client/CLAUDE.md` via *Use when*. Empty for now — add a file per
topic as the need arises.
