# docs — reviewer-core

Deep-dives for the `reviewer-core` engine: the review pipeline, the grounding
gate, prompt/finding contracts. Linked from `reviewer-core/CLAUDE.md` via
*Use when*. Empty for now — add a file per topic as the need arises.
