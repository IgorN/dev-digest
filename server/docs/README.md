# docs — server

Deep-dives for the `server` package: pipeline walkthroughs, schema/diagram notes,
design rationale that's too long for `server/CLAUDE.md`. Linked from
`server/CLAUDE.md` via *Use when*. Empty for now — add a file per topic as the
need arises.
